<!DOCTYPE html>
<html>
<head>
  <title>Cuayolo</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <section class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Cuayolo", root_url, class: "navbar-brand" %>
    </section>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <section class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "Casa", root_url %></li>
      </ul>
      
      <ul class="nav navbar-form navbar-right">
        <%= form_tag sessions_path, html: { role: "form", class: "form-inline" } do %>
          <article class="form-group">
            <%= text_field_tag :username, nil, class: "form-control input-sm", placeholder: "Nombre de usuario" %>
          </article>
          <article class="form-group">
            <%= password_field_tag :password, nil, class: "form-control input-sm", placeholder: "Contraseña" %>
          </article>
          <article class="form-group">
            <%= submit_tag "Entrar", class: "btn btn-sm btn-success" %>
            <article class="form-group visible-xs">
              <%= link_to "Registrarse", :login, class: "btn btn-sm btn-primary" %>
            </article>
          </article>
        <% end %>
      </ul>
    </section><!-- /.navbar-collapse -->
  </nav>

  <br><br><br>

  <section id="body" class="row">
    <% if flash.count > 0 %>
      <article id="row">
        <article class="col-md-9 col-md-push-1">
            <% flash.each do |name, msg| %>
              <%= content_tag :article, msg, :id => "flash_#{name}", class: "alert alert-#{name}" %>
            <% end %>
        </article>
      </article>
    <% end %>
  </section>

  <section class="row">
    <%= yield %>
  </section>
  

</body>
</html>
